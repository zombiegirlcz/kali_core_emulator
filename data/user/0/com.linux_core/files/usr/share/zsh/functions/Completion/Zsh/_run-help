#compdef run-help
local d expl
local HELPDIR=${HELPDIR:-/data/user/0/com.linux_core/files/usr/share/zsh/5.9.2/help}
[[ -d $HELPDIR ]] && {
	d=($HELPDIR/*(:t))
	(($#d)) && d+=('.' ':') && _wanted commands expl 'command' compadd -a d
} || _man
