# Gradle JMV a build vlastnosti (AGENTS.md: parallel VYPNOUT, stabilní build)
# heap 4 GiB pro Kotlin compile + kotlin dsl (Modal build má memory=8192)
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.parallel=false
kotlin.incremental=false